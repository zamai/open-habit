# Keep data local and sync through private iCloud

The app stores its data locally so every tracking flow works offline, then synchronizes through the person's private iCloud database when available. It will not require an app-specific account or a hosted data service. This gives iPhone and iPad users cross-device sync and reinstall recovery while keeping the product tied to Apple's ecosystem; exported files remain the user-controlled backup rather than promising versioned recovery from synchronized deletions.
